{% extends 'base.html.twig' %}

{% block title %}Profil{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ duck.duckname }}! ✅</h1>
</div>

<div>
    <table class="table">
        <tbody>
            <tr>
                <td>Name :</td>
                <td>{{ duck.firstname }}</td>
            </tr>
            <tr>
                <td>Lastname :</td>
                <td>{{ duck.lastname }}</td>
            </tr>
            <tr>
                <td>Email :</td>
                <td>{{ duck.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('profil_edit') }}">Modify profil</a><br>
    <a href="{{ path('profil_edit_password') }}">Modify password</a><br>
</div>
    <a href="{{ path('quack_index') }}">back</a>

{% endblock %}
